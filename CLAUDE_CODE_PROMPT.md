Read `FOR_LOCAL_CLAUDE.md` in this repo's root folder and follow it exactly,
start to finish, in order — Step 1 (push the backlog), then Step 2 (write
and push `REMINTH_STATE.md`), then Step 3 (tell me the commit hash).

Also read `CLAUDE_CODE_HANDOFF.md` in the same folder first, before you
start — it has the context from tonight's testing (the sign-in finding, what's
already confirmed working, what's still untested, the known gaps). Don't
redo work it already marks as VERIFIED tonight; build on it instead of
rediscovering it from scratch.

Follow the rules in FOR_LOCAL_CLAUDE.md precisely, especially: label every
claim VERIFIED / ASSUMED / UNTESTED, never paste real tokens/uuids/client
IDs into anything you write or print (redact them), paste raw log lines
instead of summarizing them, and don't force-push or rewrite history on
main.
